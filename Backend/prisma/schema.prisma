
// =========================
// PRISMA CLIENT
// =========================

generator client {
  provider = "prisma-client-js"
}

// =========================
// DATABASE
// =========================

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}


model Users {
  id Int @id @default(autoincrement())
  email String
  name String
}
